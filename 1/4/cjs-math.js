// 有効

// module.exports.add = (a, b) => a + b;
// module.exports.subtract = (a, b) => a - b;

// module.exports = {
// 	add: (a, b) => a + b,
// 	subtract: (a, b) => a - b,
// };

// exports.add = (a, b) => a + b
// exports.subtract = (a, b) => a - b

// 無効

// exports = {
// 	add: (a, b) => a + b,
// 	subtract: (a, b) => a - b,
// }
